# 魔法世界文字 MUD 产品需求书

> 版本：0.1
> 状态：产品方向已确认，待交互原型与技术方案评审
> 基础项目：SillyTavern
> 发布范围：个人使用 / 小范围模组分享

## 1. 产品定义

### 1.1 一句话描述

一款以角色关系为核心、由规则系统和多级 AI 共同驱动的持续世界文字 RPG。玩家以原创角色进入 1991 年的霍格沃茨，可以自由行动、结识或得罪任何人，并通过可追溯的因果改变原著时间线。

### 1.2 核心玩家幻想

玩家不是跟随哈利完成一遍原著，而是在可信的魔法社会中拥有另一段人生：

- 以原创身份进入原著世界。
- 自由建立友情、朦胧情感、竞争、师生和家庭关系。
- NPC 有自己的目标、秘密和底线，可以拒绝、欺骗、疏远或永久敌对玩家。
- 原著人物和事件真实存在，但不会围绕玩家暂停，也没有不可改变的主角光环。
- 好奇、有趣且符合情境的魔法用法会得到系统鼓励。
- 失败、重伤、退学、死亡和关系破裂都可能成为永久世界事实。

### 1.3 产品支柱

1. **自由扮演**：以自然语言描述行动，不受固定选项限制。
2. **可信关系**：人物不是好感度售货机，关系允许矛盾、误解和不可逆变化。
3. **规则托底**：属性、技能、物品、地点、时间和骰子保证结果不是 AI 随口决定。
4. **持续因果**：玩家缺席时世界仍然推进，原著事件也受条件和因果约束。
5. **分级 AI**：不同成本和能力的模型承担不同职责，规则引擎拥有最终状态写入权。

## 2. 产品范围

### 2.1 长期愿景

长期版本允许玩家：

- 选择魔法世界中的任意时间点。
- 以学生、教授、傲罗、职员、普通巫师或其他原创身份接入。
- 使用不同年代的世界蓝本和开局模板。
- 一个角色退场后，以新角色进入同一条已经改变的时间线。

长期能力通过“世界包 + 时间线快照 + 身份模板”实现，不在核心逻辑中写死 1991 年或学生身份。

### 2.2 MVP

MVP 只交付一个完整垂直切片：

- 时间：1991 年。
- 身份：原创霍格沃茨一年级新生。
- 内容跨度：角色创建、收到入学通知、对角巷、霍格沃茨特快、分院仪式、开学后第一个月。
- 平台：桌面网页优先。
- 内容结构：日程沙盒 + 多条并行事件线。
- 原著人物：按日程和动机自然存在，不强制玩家进入哈利的朋友圈。

### 2.3 MVP 不包含

- 完整七学年。
- 任意年代和成人职业的实际可玩内容。
- 全校每个 NPC 的持续实时模拟。
- 多人联机。
- 商业化素材和官方影视资源。
- 无约束地让 AI 重写魔法底层规则。

## 3. 核心循环

1. 玩家查看当前场景、时间、人物和已知线索。
2. 玩家自由输入说话、行动、施法或调查；移动通过 `→【地点】` 或候选确认显式授权。
3. 系统解析意图并判断是否需要判定；自然语言中的地点提及只能产生移动候选，不能直接修改位置。
4. 无风险日常行动直接由低端 AI 演绎。
5. 有风险且结果不确定的行动进入规则判定。
6. 重要判定由玩家手动投掷 3D 骰子；被动感知和 NPC 行动使用暗骰。
7. 系统原子提交现场正文与权威状态；人物即时印象和共同记忆是独立的可选建议，校验失败只丢弃对应建议。中端 AI 只在日结、节奏、章节封存或低端明确提交“局部事件已结束”边界时介入，高端 AI 在必要时处理复杂因果。
8. 低端 AI 根据已提交的结果生成叙事和对话。
9. 玩家主动结束场景后，由中端或高端 AI 提交结构化切场事务，旧场景封存为只读档案并自动保存。

## 4. 开局与角色创建

### 4.1 创建流程

角色创建采用结构化问答，不要求玩家一次填写完整人物卡。

1. 基本身份：姓名、性别表达、固定出生日期、当前年龄、族裔/可见文化线索、外貌与当前呈现等。
2. 家庭背景：血统、监护人、家庭环境、经济条件和成长地点。
3. 性格与经历：欲望、恐惧、习惯、重要关系和过往事件。
4. 能力倾向：学习方式、体能、观察、社交和压力反应。
5. 魔法天赋：总体潜质、领域倾向和玩家选择的罕见天赋。
6. 故事偏好：故事口味、关系重点、社交密度、相对年龄偏好和 Canon 浓度。
7. AI 将结构化答案润色为连贯背景。
8. 玩家可修改润色文本和结构化字段。
9. 确认后生成初始人物卡与隐藏成长线索。

AI 润色不得擅自改变玩家已确认的事实。
故事偏好用于人物推荐和场景编排，不属于玩家角色性格，也不保证 NPC 必然接受某种关系。

### 4.2 六项基础属性

- **体魄**：力量、耐力、抗伤和长时间活动。
- **灵巧**：反应、协调、精细动作和飞行操控。
- **感知**：观察、直觉、环境意识和识破异常。
- **智识**：理解、记忆、推理和理论学习。
- **意志**：专注、勇气、自制和精神抵抗。
- **魅力**：表达、说服、气场和社会影响。

基础属性采用 1–20，10–11 代表普通水平。判定修正为：

`向下取整((属性值 - 10) / 2)`

因此 1 点为 -5，10–11 点为 +0，20 点为 +5。基础属性描述通用能力，不直接等同于魔法强度。

初始属性由背景问答生成，玩家可以在总点数预算和单项上限内调整。系统应同时展示数值和自然语言解释，避免玩家必须先理解数值构筑才能完成人物创建。

### 4.3 魔法天赋

天赋与技能分离：

- **总体潜质**：影响魔法学习速度、复杂魔法的稳定性和成长上限，不直接为所有施法提供通用加值。
- **领域倾向**：例如魔咒、变形、魔药、草药、飞行、心灵、防御或黑魔法抗性。优势领域初始技能较高、成长较快，弱势领域相反。
- **罕见天赋**：例如蛇佬腔、易容马格斯和预言倾向，由玩家自由选择并写入背景。
- **未知潜质**：部分天赋不在开局完整公开，通过课程、魔杖和事件逐步发现。

结构化问答先生成有长短板的天赋方案，玩家可在确认前调整。罕见天赋必须带来叙事责任、风险或社会后果，不能只作为无代价加成。

### 4.4 技能成长

技能采用 1–20，代表当前掌握程度。判定时使用有限加值：

`向下取整(技能值 / 4)`

对应关系为 1–3 点 +0、4–7 点 +1、8–11 点 +2、12–15 点 +3、16–19 点 +4、20 点 +5。

技能通过以下方式积累：

- 上课与完成作业。
- 阅读和请教。
- 主动练习。
- 实战使用。
- 对失败进行复盘。

只有具备实际难度、有效反馈或新理解的活动才产生技能进度。机械重复同一低难度动作的收益会快速衰减。失败可以提供经验，但必须有复盘、反馈或新的尝试。

技能达到阈值后提升。专长、罕见能力的深化和关键突破可由剧情条件解锁。

### 4.5 分院

分院帽根据人物卡、开局行为和对话提出学院建议，并解释理由。玩家可以接受建议，也可以坚持自己的选择。最终选择一经提交即成为世界事实。

## 5. 规则与判定

### 5.1 基础骰制

采用 D20 加值制：

`D20 + 基础属性修正 + 技能修正 + 装备/魔杖修正 + 情境修正`

优势时投掷 2D20 取高，劣势时投掷 2D20 取低。优势与劣势互相抵消，同类来源不无限叠加。

结果与隐藏难度或对抗方结果比较。角色正面对抗时双方分别进行 D20 判定，NPC 骰默认隐藏。玩家默认看到：

- 使用了哪项属性和技能。
- 可感知的优势、劣势和状态影响。
- 骰面结果和最终结果等级。

玩家默认看不到：

- 精确难度值。
- NPC 的隐藏能力。
- 暗骰结果。
- 尚未发现的环境因素。

### 5.1.1 单次生成判定链

MVP 的判定在低端现场表演请求之前由本地规则层完成，不使用额外 AI 调用：

1. 本地动作分类器识别物理对抗、灵巧、感知、智识、意志、社交和施法风险。
2. 普通的确定性动作不触发判定；玩家点击“主动判定”时强制生成一次最匹配的判定。
3. 规则层使用安全随机源生成玩家 D20；正面对抗同时生成 NPC 暗骰，环境行动使用难度档位对应的隐藏 DC。
4. 规则层计算属性、技能、装备、情境、优势/劣势和最终结果等级，形成不可由模型修改的 `checkResolution`。
5. `checkResolution` 在请求低端模型前写入玩家消息。失败重试和页面恢复必须复用原骰值。
6. 低端模型在同一次现场生成中依据既定结果续写，不得重掷、修改难度、弱化失败或泄露 NPC 暗骰。
7. 完成后，判定和现场叙事作为同一回合事务提交，并在对话流保留判定卡。

### 5.2 结果等级

判定至少支持：

- 灾难性失败。
- 失败并产生后果。
- 成功但付出代价。
- 完全成功。
- 重大成功。

失败不必让剧情停止，但允许产生永久后果，包括重伤、退学、死亡、暴露秘密和关系破裂。

天然 1 将结果等级降低一级，天然 20 将结果等级提高一级，但不能让逻辑上不可能的行动自动成功，也不能让必然行动凭空失败。

### 5.3 状态与伤势

生存状态采用“短状态轨 + 具体标签”：

- **体力轨**：连续活动、奔跑、战斗和缺乏休息带来的消耗。
- **压力轨**：恐惧、冲突、精神攻击和长期紧张带来的负担。
- **伤势标签**：例如擦伤、骨折、中毒、诅咒或魔力失控，记录具体影响、治疗条件和持续时间。

状态轨用于表达当前承受能力，伤势标签用于改变具体行动条件。状态耗尽不会只做简单数值归零，而是触发昏迷、失控、崩溃或其他符合情境的严重后果。

### 5.4 创意行动

创意行动采用“规则计算 + AI 合理性评估”：

1. 系统提取玩家的目标、方法、资源和预期效果。
2. 规则层检查角色是否具备必要知识、物品和行动条件。
3. 中端或高端 AI 评估魔法活用是否符合已知规则及当前情境。
4. 有趣且合理的做法可以获得优势、降低难度、改变判定属性或创造新效果。
5. 创意描述不能无视角色能力、已提交事实或必要代价。

### 5.5 判定复核

- 玩家提交行动后，本地骰值立即成为该回合的权威事实。
- AI 输出校验失败时可以重新生成叙事，但必须复用原判定，不能重掷。
- 玩家消息必须在模型请求前持久化。若低端请求或结构校验失败，原消息下方显示“回复生成失败，玩家消息已保存”以及“重试本回合”；重试直接复用原消息、移动与骰值，不得追加重复玩家消息。
- 存在失败且未结算的玩家回合时，输入框暂时锁定，防止下一条行动跨过失败回合。刷新页面不得自动消耗 Token 重试，必须由玩家明确点击。
- 玩家希望修改行动意图时，需要撤回尚未提交的整个回合；不能只保留有利骰值。
- 骰值、结果和状态变更作为一个事务写入世界。
- 已提交结果不能在当前时间线中无痕覆盖。
- 玩家可以复制世界，基于旧提交点建立新分支。

### 5.6 3D 骰盘

重要判定最终可使用中央临时覆层表现已经由规则层落定的骰值：

1. 展示判定属性、技能和可见修正。
2. D20 自动播放物理滚动；玩家可以跳过，也可以在设置中改为手动释放。
3. 3D 组件必须落到规则层预先指定的骰面，不负责产生随机结果。
4. 骰子落定后展示结果等级。
5. 覆层收起，对话流保留一张可展开的判定卡。

被动判定和 NPC 暗骰不播放完整动画。

当前 MVP 先使用对话流 D20 判定卡，不阻塞低档续写。候选 3D 组件优先使用 `@3d-dice/dice-box`。规则引擎负责随机数和权威结果，3D 组件负责表现指定结果；视觉动画不得成为状态真相来源。必须提供低性能模式、跳过动画和无 WebGL 的文本回退。

## 6. 魔法系统

### 6.1 施法条件

玩家施法使用结构化标记 `✦【咒语:stable_spell_id】`。标记可以出现在动作或台词块中，只要存在就必须在低档表演前触发一次本地 D20 施法判定。公开叙事使用自然语言和原文咒文，不向模型暴露本地标记。

施法结果综合考虑：

- 是否知道该魔法及正确方法。
- 对应课程技能和熟练度。
- 基础属性。
- 魔法总体潜质及领域倾向。
- 魔杖适配度和当前装备。
- 情绪、专注、伤势和疲劳。
- 环境、距离、目标和干扰。

### 6.2 学习过程

魔法不是达到等级后自动解锁。标准流程为：

1. 接触知识。
2. 理解原理和动作。
3. 进行不稳定尝试。
4. 通过练习提高熟练度。
5. 在压力环境下验证掌握程度。
6. 形成个人化用法。

咒语目录的 `curriculumYear` 只记录霍格沃茨常规教学年级，不是学习、尝试或施放门槛。玩家可以在任意年级去图书馆自学、接受私下教授或直接实验高年级乃至禁忌咒语；系统通过难度、熟练度、风险、法律后果和叙事后果结算，而不是阻止输入。

玩家咒语本记录课堂教学、自学、私授和自行实验四类来源，并保存尝试次数、成功次数、重大成功、失败、熟练度 XP 与等级。已学咒语进入输入框快捷栏；完整目录始终可从“尝试未学咒语”访问。

## 7. 时间、地点与日程

### 7.1 场景制时间

- 重要互动逐段游玩。
- 普通移动、课程和等待消耗明确时间。
- 普通回合的 15 分钟是最低耗时，不是固定上限。现场表演超过当前时长的软内容容量时，规则层按 15 分钟档增加实际耗时，不得因为内容较长要求模型删减或重试。
- 同一小地图内的房间移动由本地规则沿出口图校验并立即提交，不调用中端；玩家与每名 NPC 分别保存权威房间。
- 出口条件必须支持方向性。口令、门卫问答和入场许可默认只约束配置的进入方向；反向离开是否受限由 `reverseConditions` 独立声明。四个学院公共休息室允许成员无口令离开，但从公共区域进入仍需对应凭证。
- 玩家可以跳过数小时或数天。
- 跳过期间由系统结算课程、关系、身体状态和活动事件。
- 事件不会为了等待玩家而暂停。
- 权威时钟必须通过本地确定性规则派生 `behavioralEnvironment`，而不是只作为 UI 标签。环境至少包含时段、日照、室内/室外暴露、宵禁、睡眠压力、区域天气、温度和行为约束；天气以日期、区域和六小时时段形成稳定槽位，同一时间地点刷新不得变化，也不得增加模型调用。
- 普通回合和转场开场必须把相关环境作用于人物作息、音量、衣着、取暖、遮蔽、路线、视野、舒适度或计划。室内场景不得让雨雪直接出现在室内，只能通过窗户、声音、穿堂风、湿衣物、取暖和室外计划表现。当前场景、位置和环境必须覆盖已经过期的每日地点或活动指令。

### 7.2 地图

地图展示：

- 已知地点。
- 当前地点与可达路径。
- 预计移动时间。
- 门禁、宵禁和临时封锁。
- 玩家当前知道的公开活动。

玩家可以通过“→ 移动”快捷目录选择目的地、输入 `→【地点】`，也可以自由输入“寻找密道”“跟踪某人”等非标准移动。快捷目录只展示当前小地图中从当前位置有合法路径、且当前身份可见的地点，按楼层分组、当前层优先并支持搜索；选择结果只写入结构化标记，不立即移动。普通文本中出现地点名时，系统只提出候选并要求玩家确认；“我之前去过古灵阁”“我奶奶带我去过古灵阁”等回忆或对话不得自动移动。未知地点需要通过探索、线索或他人带领发现。

玩家明确接受由 NPC 带路时，不要求玩家本人预先知道或说出目标地名。规则层应从带路人的公开活动和最近已提交的公开场景记录中解析目标，只接受当前地图内明确存在、非当前且路径可达的房间；不能把地图级名称自动降级为默认房间，也不能让低端模型猜测后只移动 NPC。

预设地图可以为房间绑定地点固有角色，例如商店经营者、常驻职员或固定教师。玩家进入对应房间后，规则层必须在任何现场表演前幂等地把固有角色加入人物库和该房间的权威人物状态。地点固有角色不是节奏监督器制造的“新角色”，不得因为“新增角色是最后手段”而延迟正常出场。

列车、船只、车辆、宿舍等具有稳定内部结构的容器地点可以在父地图中只保存入口节点，并标记为需要独立内部图。玩家首次进入且本地没有绑定图时，中端场景制图器可以现场生成 1–4 个分区、2–16 个房间和完全连通的出口；规则层校验稳定 ID、坐标、连通性和当前房间后，将整图保存到 `customLocalMaps`，并用 `interiorMapBindings[parentMapId:parentRoomId]` 永久绑定。中端只能补齐稳定建筑/交通拓扑，不能借制图创建人物、物品、线索、秘密或剧情事件。

室内制图属于成功转场后的非阻塞空间补全。转场核心已经提交后，生成或翻译失败只能记录 `interiorMapGeneration.status = failed`，不得回滚场景、人物或位置；同一失败 binding 不得在每次刷新时自动重试。生成成功或已有 binding 时，系统必须进入绑定图、迁移同房人物和随身物品，并将主小地图与地图检查器恢复为“跟随当前位置”；玩家主动浏览其他地图的选择不受限制。

### 7.3 日程

关键 NPC 拥有：

- 基础课程与作息。
- 当前短期计划。
- 关系驱动的临时安排。
- 活动事件产生的日程变化。

系统持续模拟重点角色和活动事件。普通 NPC 在首次进入相关场景时，根据世界事实和日程模板补全。

### 7.4 长转场世界推演

- 场景切换必须提交绝对权威 `nextClock`。当 `nextClock - currentClock` 达到至少 7 个完整日时，同一次转场导演调用必须结算场间世界变化；不得为此额外调用第二个模型。
- 世界变化包含 1–4 条《预言家日报》边角新闻。新闻只使用公开或已提交事实，发生时间必须位于跳过区间内；中端不得借新闻创建永久后果、泄露锁定线索或改写原著重大事实。
- 旧场景同房间见证者可以从亲历的公开事件创建流言包。流言包分别保存私有真相底稿、当前流传版本、来源人物、接收人物、传播渠道、目标群体和 1–3 级失真。
- 新流言只能以一级失真开始。已有流言只能由确实知道该版本的人物继续传播，每次增加一级失真；未被记录为见证者或接收者的 NPC 不得自动获得该流言。
- 传播渠道固定为同学、家庭、学院、教职员、本地和公开流传。匿名群体写入目标群体文本，重点人物接收必须使用 `actorLibrary` 现有 ID。
- 流言连续 28 天未传播后自动淡出。真相底稿不进入玩家可见卡片，也不进入不知情 NPC 的低端上下文。
- 下一场景开场前显示“场间世界回声”编辑卡：上半是日报边角栏，下半只展示当前流传版本、渠道、目标群体与失真层级，用于建立场景之间的连续感。
- 不足 7 天的普通转场必须返回空 `worldChanges`，不得无故制造新闻或流言。

### 7.5 Canon 人物目录与故事人物预算

- 系统维护独立于时间线人物库的离线 Canon 身份目录。723 行英文来源快照与简中名称/别名注册表分开版本化；已确认属于同一人的长名、短名或重复来源行必须通过 redirect 收敛到一个稳定 ID，运行时不得依赖外部 API。
- 全局 Canon 目录不计入故事人物上限，也不得整表注入 Prompt。自动候选只能来自完成权威简中名称、别名与身份去重的可入场目录；未本地化的英文记录保留显式检索能力，但不得自动进入中文 UI。规则层按玩家点名、当前年代、地点、故事偏好、相对年龄和固定 Canon 设定标签评分，只提供有限候选。
- 每条时间线保存 `castPolicy`。默认故事人物库上限为 48，其中由节奏器原创的公共过场人物最多 12 名；目标近龄关系人物为 16 名，教师、父母和其他权威人物最多 12 名。人物只有实际进入 `actorLibrary` 后才占用槽位。
- 近龄人物按故事进度分阶段补足 3、6、16 名。中端选择人物时优先处理关系缺口，再比较年龄与偏好匹配、人物曝光、连续出场惩罚、Canonical 合理性和原创人物额度；当前人物不因已经在场自动获得最高优先级。
- 权威人物完成具体职责后进入叙事冷却。近龄人物不足且同一权威人物连续主导至少两个场景时，中端不得继续 `hold`，必须引入一名与玩家当前年龄相近的人物。
- Canon 人物加入后必须使用目录稳定 ID，中文全名、英文全名、常用短名、旧译名与无间隔写法必须解析到同一身份。低端不得把可识别 Canon 写成 `temporary_actor`；若其输出发生 ID 漂移，Reducer 必须在不追加模型调用的情况下重写到已入库稳定 ID。故事人物预算耗尽后不得再引入新 ID。
- Canon 的原始出生文本和 `settingTags` 属于固定设定数据。本地校准层可为重点人物覆盖冒险、稳健、求知、社交、恶作剧、野心、同理心、谨慎、叛逆和神秘倾向；其余人物使用学院、职业和技能的确定性回退。特定故事口味可以要求至少命中一个固定标签。
- 玩家与 NPC 的出生日期或 Canon 出生范围一经建立永久固定。`same_age / younger_peer / older_peer / older_generation` 等年龄层必须根据当前世界时间和玩家当前年龄动态派生，不得保存为 NPC 静态标签。
- `friend / rival / romantic_interest / mentor / family / enemy` 是关系标签，不得混入 `settingTags`。未出场人物没有关系标签；人物实际出场时只能根据已经成立的关系事实初始化，陌生人默认从 `acquaintance` 开始。
- 建档流程保存 `storyPreferences`：故事口味、关系重点、社交密度、相对年龄偏好和 Canon 浓度。它们只影响人物介绍机会，不保证友情、恋爱、敌对或其他关系结果。

## 8. 世界与原著因果

### 8.1 开局原则

- 1991 年的公开世界状态默认忠于原著。
- 不在创建世界时暗中加入随机历史偏移。
- 原创玩家角色是初始状态中唯一必要新增变量。
- 玩家可以直接利用自己掌握的原著元知识。
- 元知识不会自动变成可信证据；NPC 会依据身份、关系和证据决定是否相信玩家。

### 8.2 世界事实分层

1. **底层规则**：魔法基本规律、世界结构和系统约束，默认锁定。
2. **初始原著事实**：开局时成立，但可被游戏内可解释的因果改变。
3. **局部事实**：原创人物、具体关系、支线地点、传闻和临时事件，可由 AI 动态生成。

AI 可以提出新增、修改或删除事实，但必须输出：

- 目标事实。
- 修改类型。
- 触发原因。
- 证据和前置条件。
- 受影响的人物、事件和地点。
- 是否与更高层事实冲突。

规则层校验后才能提交。事实删除实际采用失效或被推翻记录，不物理抹除历史。

### 8.3 原著事件

原著事件是带条件的世界事件，不是强制播放的章节。玩家改变前置条件后，事件可以：

- 按原方式发生。
- 延迟发生。
- 由不同人物触发。
- 产生不同结果。
- 因条件不再成立而取消。

玩家没有参与的事件照常演化，由相关 NPC 的能力、动机、资源和骰子决定结果。

### 8.4 角色退场

角色死亡、退学或永久失去行动能力后：

- 当前角色结局固化。
- 世界继续推进。
- 玩家可以创建新角色进入同一时间线。
- 新角色只能获得符合其身份的信息，不能自动继承旧角色的全部知识和关系。

## 9. 关系与 NPC

### 9.1 多维关系

每个方向分别记录：

- 信任。
- 亲密。
- 尊重。
- 恐惧。
- 吸引。
- 怨恨。

关系是非对称的。A 信任 B，不代表 B 信任 A。一个 NPC 可以同时亲近、嫉妒并不信任玩家。

### 9.2 关系变化

关系变化必须绑定具体原因：

- 共同经历。
- 承诺与违约。
- 公开行为和私人行为。
- 谣言与误解。
- 利益冲突。
- NPC 自身价值观和秘密。

关系不能只通过重复送礼或选择迎合台词稳定刷高。

### 9.3 展示方式

正常界面只显示定性描述，例如“谨慎信任”“明显戒备”“亲近但失望”。精确数值仅在可选调试视图中展示。

### 9.4 NPC 内部状态

NPC 至少包含：

- 公开身份与外在表现。
- 性格、价值观和底线。
- 长期欲望与短期目标。
- 独立记忆。
- 对不同角色的关系向量。
- 已知事实和错误认知。
- 隐藏秘密。
- 当前意图与情绪。
- `lifeStatus`：`alive / injured / incapacitated / missing / dead`。
- 公开状态说明、状态起始时间和 `lifeStatusPermanent` 永久标记。

真实秘密与对外说法分开存储。叙事模型只能获得当前视角有权知道的信息，不能只依赖提示词要求模型“假装不知道”。

生命状态在转场结算时由转场导演统一复查。每条 `actorState` 必须显式输出状态、永久标记和公开说明；中端可以依据已发生事实提交或解除受伤、失能、失踪等可逆状态。永久状态属于高端世界导演权限；永久死亡必须同时满足 `lifeStatus=dead`、`lifeStatusPermanent=true`、`present=false` 和具体公开说明。永久死亡人物不能被普通场景切换、地点居民自动准入或低端表演恢复。

### 9.5 记忆与传闻

NPC 记忆采用“事件记忆 + 阶段摘要”：

- 重大事件、第一次经历、承诺、背叛、重要礼物和强烈情绪作为事件记忆长期保留。
- 高频日常互动可以压缩为阶段性关系摘要。
- 细节会随时间淡化，反复提及或情绪强烈的内容更牢固。
- NPC 的性格、偏见和后续信息可以改变其对记忆的解释，但不会直接改写权威事件日志。

每条事件记忆支持“受保护细节锚点”，例如具体礼物、原话、地点、在场者、第一次发生的标记和当时的情绪。压缩器可以缩短表述，但不得删除、泛化或替换这些锚点。比如“第一次收到某根亲手雕刻的羽毛笔”不能被压缩成“收到过礼物”。

#### 9.5.1 动态印象与三层共同记忆

每个重点 NPC 在权威 `actorLibrary` 中保存：

- `firstImpressionOfPlayerEn / firstImpressionOfPlayer`：NPC 第一次在场看见玩家时形成的 4–24 词主观观察快照。它必须结合玩家可见的年龄、五官、肤色、发型、身形、穿着、姿态或明确可见的文化线索，以及该 NPC 自己的性格和注意重点；一旦写入不可覆盖。
- `impressionOfPlayerEn / impressionOfPlayer`：该人物心里对玩家的一句主观反应，建议 3–12 个英文词或等量中文，例如“我那不省心的女儿啊”。它不是身份标签、证据列表、当前活动或本轮事件摘要。
- `sharedMemories.core`：最深刻的共同经历，最多 3 条，只能由中端整理器晋升或合并。
- `sharedMemories.recent`：近期大事，最多 6 条，仍直接影响当前判断。
- `sharedMemories.everyday`：日常小事，最多 8 条，用于保留相处质感。

初见快照与行为驱动的当前印象是两条独立状态。家人、监护人、亲属和既有朋友必须在人物库初始化时，根据玩家已确认背景与双方既有共同生活生成短而主观的当前印象；新认识的人物则在首次能看见玩家时形成初见快照，后续行为只能改变当前印象，不能重写第一眼。只有真正尚未见面的角色可以使用未形成印象状态；亲人和老朋友不得初始化为 `stranger`、`unknown` 或单纯身份关系标签。

族裔/文化字段只表示玩家明确选择公开或可观察的身份线索。NPC 可以依据时代、个人经历和性格产生不同注意与偏见，但不得把族裔直接推导为隐藏血缘、国籍、阶级、能力、道德或套用通用刻板印象。听见但看不见玩家时不得生成外貌初见。普通当前印象变化至少间隔 3 个已提交回合，没有形成持久态度变化时必须省略更新。

低端现场表演者可以在一次普通回合中，为亲自参与或目击该回合的 NPC 提交：

- 一条仅在持久态度确实变化时形成的短印象；不得复述本轮动作。
- 至多一条共同记忆候选，显著度只能是 `everyday` 或 `notable`。

低端不能直接写入近期大事、最深刻记忆、隐藏事实、人物秘密、未亲眼见证的推断，也不能改写静态身份关系。所有低端候选先进入日常层。`notable` 只表示候选具备晋升资格，并且必须附带 24 个英文词以内的 `lastingImpactEn`，说明其如何持续影响信任、义务、忠诚、恐惧、权限、安全、冲突或未来选择。购物、吃饭、等待、赶路、玩笑、普通发脾气、小道歉和小尴尬默认不得标记为 `notable`。

初见印象、当前印象和共同记忆候选不得进入玩家回合的致命失败边界。规则层分别校验视听权限、已有初见、更新冷却、字数、显著度、字段白名单和 `lastingImpactEn`；非法候选仅删除自身，合法的 segments、公开事件、人物活动、人物在场集合、移动、判定和程序推进继续提交，且不得为了修复可选元数据再次调用低端模型。首次见面未得到合格初见时保留 pending，不阻断当前 Scene。

低端现场表演者可以在局部事件、话题、冲突、差事或实际任务明确告一段落时提交 `signals.eventEnded:true`；它不封存场景，也不得因为回复结束或固定时间步进而触发。信号缺失默认事件继续，不进入致命失败边界。事件边界存在、确有尚未整理的共同记忆，且事件结束回合距离上次中档整理至少 10 回合时，系统才调用一次中端共同记忆整理器。冷却期内的旧边界不能在第 10 回合被定时补触发，必须等待冷却满足后的下一个事件结束信号。中端只能引用已有 memory ID，负责：

- 合并或遗忘重复日常。
- 只有已有近期记忆，或带明确长期影响的 `notable` 候选，才能生成近期大事；普通日常不能因数量多而升级。
- 将已有近期大事或深刻记忆提炼为长期核心记忆。
- 遗忘冗余的日常/近期记录。
- 根据已存在的共同经历收束人物当前印象。

深刻记忆不能直接遗忘或降级。中端整理失败时保留全部低端记忆并进入冷却，不得让已经完成的玩家回合失败。关系记忆 v6 迁移会把历史上由低端直接写入的近期记录降回日常候选，把过长或空泛的旧印象收束成短 shorthand，并继续清理无见证资格的旧迁移记录。

玩家行为通过见证者网络传播：

1. 目击者形成包含自身理解和偏差的记忆。
2. 目击者依据动机、关系和风险决定是否传播。
3. 传播内容保留消息来源和可信度。
4. 接收者根据自己与传播者、玩家的关系决定是否相信。
5. 传闻可以被质疑、修正、夸大或反向利用。

“永久敌对”表示关系不会仅因时间流逝或普通正面互动自动恢复。重大补偿、共同危机或长期可信改变可以重新打开关系，但门槛必须与原伤害相匹配。

### 9.6 情感内容

系统不强制指定玩家的关系方向。低年级 NPC 的主动表现应符合角色年龄和当前关系背景；玩家可自由决定其角色的表达和行动，世界依据人物边界、社会环境及后果作出反应。

### 9.7 MVP 重点角色池

每局激活 12 名重点 NPC：

- 10 名原著角色。
- 2 名每局完全生成的原创角色。
- 原著角色包含固定核心位和学院相关替换位。
- 原创角色根据玩家学院和既有角色空缺生成，优先承担舍友、直接竞争者或事件关键人的功能。

原著人物的初始性格、偏见、目标和关系忠于 1991 年状态，之后允许被持续且有分量的经历逐步改变。普通礼物或几次迎合对话不能快速重塑经典人物。

原创角色虽然每局生成，但必须通过结构校验：目标、底线、秘密、家庭影响、与至少一条事件线的关系，以及与现有重点 NPC 的差异均不可为空。

### 9.8 权威物品栏

每个物品必须保存稳定 ID、名称、说明、重要度、持有者、状态、取得时间和保管方式：

- `importance`：`key / important / ordinary`。
- `custody`：`carried / equipped / stored / consumed / lost`。
- 随身与装备物品随玩家移动；存放物品保留原 `mapId + roomId`。
- 每次场景初始化在 `scene.itemStates` 保存所有物品的保管快照。

魔杖、钥匙、信件、日记、地图、许可证、信物、传家宝和魔法制品等耐久重要物品，一旦所有权在正文中明确完成，当前回合必须原子写入物品栏。食物、糖果、饮料、包装、收据和一次性小物默认不记录；只有玩家明确表示保留、携带或存放时才进入物品栏。

## 10. 任务与事件

### 10.1 展示

不使用传统的“击败 5 个目标”式任务列表。玩家看到的是：

- 已知线索。
- 对他人的承诺。
- 个人目标。
- 传闻。
- 即将到来的日程。
- 已产生的后果。

隐藏条件、真实幕后人物和标准解法不向玩家显示。

### 10.2 首月内容骨架

MVP 至少需要：

- 入学通知与家庭反应。
- 对角巷采购和魔杖选择。
- 车站与特快列车。
- 初次同学关系建立。
- 分院仪式和宿舍生活。
- 主要课程的首次体验。
- 学院内部关系。
- 宵禁、地图探索和至少一个秘密地点入口。
- 至少三条可以错过、失败或互相影响的事件线。
- 至少一条由玩家关系触发而非固定领取的事件线。
- 原著事件的早期条件在后台开始运行。

首月事件优先围绕“校园秘密”和“同学关系”。多条事件不强制汇成唯一主线，而是共享人物、时间、地点、线索和信任资源：玩家对一条线的投入会改变其他事件中人物的立场和可用机会。

首月常见风险以关系破裂、校规处分、学院声誉及轻中度伤势为主。死亡不设系统保护，但只有玩家在明确风险后主动进入极端危险，或连续忽视升级中的严重后果时才进入结果集合。

## 11. 多级 AI 系统

### 11.1 配置方式

设置页提供低、中、高三个模型槽位：

- 三个槽位允许绑定同一个模型或接口。
- 每个槽位独立保存 Connection Profile、Chat Completion Preset、Regex Preset、Context Size 和输出安全余量。
- 中档 Connection Profile 留空时可以复用低档连接，高档留空时可以复用中档连接。
- 连接复用不改变职责边界；三个槽位始终使用各自的生成参数和权限。
- Context Size 是输入窗口；输出安全余量是 API 防截断上限，不是模型每轮必须用满的篇幅预算。三档默认安全余量均为 `12000 tokens`，旧存档中的更低上限一次性迁移到 `12000`，玩家之后仍可手工覆盖。

整体调度遵循“少调用、批处理”原则。能在一次调用中安全处理的同类工作应合并处理，但生成必须在需要玩家回应、选择或行动的位置停止，不能为了增加单次输出量而替玩家作出决定。

### 11.2 低端 AI：现场表演者

负责同一游戏日内的高频现场表演：

- 在已固化的场景和事实内描写玩家已声明的动作。
- 维持环境、人物站位、感官细节和普通可观察后果的连续性。
- 根据中端 AI 当天生成的人物指令统一表演在场 NPC 的动作与对白。
- 遵守角色库中的说话方式、性格、当前意图和知识边界。

同一场景中的多个 NPC 默认由一次低端调用统一生成，以保持现场连贯并减少调用次数。低端可以生成旁白、动作、环境连续性、NPC 对白以及玩家动作直接导致的普通可观察反应，但不能替玩家添加未声明的台词、想法、意图或选择。遇到需要检定、复杂因果或永久后果的不确定行动时，只描写尝试和即时反应，不得自行裁定结果。

低端采用 narrative-first V2 协议，唯一必填输出为 1–24 个有效 `segments`。实际发生的角色活动、进出、同图移动、物品变化、临时人物和社交提示使用稀疏 `stateProposals`；事件结束、节奏完成和程序推进使用可选 `signals`。低端不得提交或重述完整世界状态快照。

低端返回后必须进入无模型调用的确定性 `Turn Settlement Graph`：接受叙事核心、折叠稀疏 proposal、校验人物/地图/物品/知识权限、生成兼容事务。服务端图不可用时使用同构本地 reducer。无效 proposal 只进入 `settlementWarnings`，不得作废合法 segments 或触发一次专为补元数据的模型修复。旧 V1 完整字段继续作为兼容输入。

四名以上 NPC 的群像场景中，玩家只是房间里的一个刺激源，不是会议主持人。低端必须组织主要回应线程、辅助插话、NPC-NPC 侧线和独立动作，让角色可以忽略部分问题、继续手头事务、互相打断或不发言；严禁把玩家的一组问题拆给 NPC 依次完整作答。该要求属于首次生成与必要修复 Prompt 的表演契约，不得仅因群像观感不佳在本地拒绝结构合法的完整回复并额外消耗一次模型调用。

群像输出预算必须随活跃实名卡司扩容。4 人以上增加目标词数与分段，6 人、15 分钟默认约为 420–860 英文词和 6–20 个分段。新增预算至少 40% 用于主要互动与程序性推进，除玩家直接影响外最多单独刻画 2 名次要实名，禁止把扩容变成逐人反应清单。事件摘要由结算图从合法旧字段或已接受 segments 生成，`sceneProgression` 仅是可选提示。

若玩家已到达 `nextSceneIntent.mapId/roomId`，入场、排队、开门、介绍、歌曲和宣布开始都属于准备态；普通 15 分钟回合应实际完成至少一个后续程序单元。一个有边界的流程阶段完成后可设置 `signals.eventEnded:true`，用于刷新下一阶段意图；缺失信号默认事件继续，不得因此重写正文。

中端转场导演必须把 `actorStates.present:true` 当作下一幕活跃实名卡司，而不是空间人口清单。拥挤公共场景通常只选择 2–4 名有直接职责的实名人物，并根据最近三幕卡司、连续出场次数、复用惩罚与当前互动选择最多 2 名连续性锚点；当当前实名卡司达到 5 人且存在高曝光人物时，导演应在因果允许时至少轮换 2 人。轮换规则属于中端决策输入，不得成为本地硬失败条件。`actorStates` 是私有结构化状态，禁止直接拼接成公开开场，也不要求低端逐个点名；低端只表演当前镜头真正需要的人物。退出镜头的人物继续存在于人物库；如其随大队改变物理位置，中端可用 `present:false + mapId/roomId` 更新后台位置。

每个公共群像场景还应保存中端生成的 `crowdDirectionEn`。低端可按该方向在旁白中描写集体人流和最多 3 个短暂的无名视觉人物，但不得为其分配 actorId、可归属对白、私密事实、持久记忆、关系或线索。玩家选中具体无名对象形成持续一对一互动时，低端通过单个 `temporary_actor` proposal 升级稳定暂定身份，结算图负责本地补齐运行态和在场集合。该升级不等待中端节奏冷却，也不增加模型调用。精确点名的附近熟人仍由规则层召回，反应后的 everyday 记忆由结算图兜底。泛指群体不得随机召回或制造人物。

直接受话必须采用结构化控制行，不再从非结构化正文推断。每段台词使用独立的 `@人物：台词` 行，公开发言使用 `@全场：台词`；同一回合可以在动作之间穿插多个目标和多段台词。非 `@` 行全部属于动作、神态、心理或环境交互，无标记输入属于开放行动或自言自语，正文中的姓名仅算提及。规则层必须在调用模型前生成有序 `playerTurnSequence`，其中台词项保存 `targetActorId`、`lineIndex`、`speechOrder` 和 `speechText`，动作项不得被模型重新解释为对白。输入区必须提供在场人物菜单、多人 recipient preview 和清除操作；人物菜单每次插入一个新台词行，清除操作只删除受话前缀并保留台词。消息 metadata 保存稳定 actor ID 与完整 blocks，供重试、导出和身份合并后回放。

同一回合仍只调用一次低端模型。每名直接受话者获得一个以稳定 actor ID 密封的知识胶囊，包含其自己的共同记忆、私有目标、流言、社交声明、关系证据与私有印象；其他 NPC 只获得公开表演档案、当前场景事实和所有潜在发言者共同见证的 RAG 记录。共享 RAG 禁止注入 actor 档案、锁定 clue 或整幕 scene transcript，只允许 provenance 覆盖全部潜在发言者的 event 等记录。模型不得从一个胶囊推导、转述或暗示另一个 NPC 知道该事实。每个直接台词块的目标必须在进入下一块前回答、拒答、回避、明确没听见、被具体事件打断或离开，旁人不能代答。多胶囊隔离属于单次调用内的强 Prompt 契约，并非按人物拆分调用的物理隔离；产品接受该取舍以保持群像表演连贯和单回合调用成本。

普通回合和转场开场必须另外接收非秘密的 actor-ID 连续性胶囊，字段仅包含 `hasMetPlayer`、已认识人物 ID、关系阶段与数值、当前印象和按 Context 预算裁剪的近期共同记忆。连续性胶囊不得包含秘密、恐惧、私有目标或不受限知识；任一人物只能使用自身胶囊。结构化“已经见过”优先于旧人物卡中笼统或陈旧的 `stranger` 文本，禁止熟人在转场后重新执行首次自我介绍，也禁止把一个人物的记忆转给另一个人物。

Canon 人物目录中的角色、学院、技能、组织经历和后期成就不得直接转换为角色当前 `knowledgeEn`。系统必须删除 `Almost everything` 等全知表述以及尚未发生的组织归属，只保留导演明确提交的当前时点事实，并为 Canon 人物加入统一的证据边界。旧存档通过独立版本迁移完成清洗。社交声明只对说话者或明确见证者可见；关系证据只对 `sourceActorId` 本人或 `witnessedBy` 见证者可见，`targetActorId` 不等于知情人。

场景临时人物使用稳定的暂定 ID，不进入正式人物库、人物预算或社交图，仅保留最多 8 条亲历记忆。离场后，中端优先从既有临时人物、正式 guestActor 与 Canon 候选中选择再次出场；同一人物必须复用原 ID 和记忆。只有叙事中出现明确姓名或唯一强证据时，才能将暂定身份合并为正式原创人物或 Canon 身份；合并沿用暂定 ID 作为历史主键并继承记忆与别名。玩家侧不显示系统合并提示，人物卡随叙事揭晓自然更新。

人物进场与离场由结算图从“回合开始在场集合 + 合法稀疏 proposal”折叠产生。低档不再提交完整名单；未被 proposal 修改的人物保持原状态。合法 `actor_exit` 应提供离场后的活动，合法 `actor_move` 只能沿同一地图现有路径移动。非法或缺失 proposal 不得使正文失败。

低端输出的分段数、旁白数和词数属于 Prompt 质量目标，不得作为本地失败条件；本地只校验 JSON 结构、actor ID、空间、在场状态、物品、检定和线索等权威边界。瞬时魔法必须由明确施法动作触发，文本仅提到“魔杖”“咒语”“magic”或“wand”仍按普通回合计时。

全局低端语调契约采用近期表达自校正，而非精确禁词或句式黑名单。模型应追踪最近回复用过的措辞、句形、节奏、段落开头与收束、比较机制、感官锚点和描述手段，主动打破平行模式，换用动作、物件、对白时机、空间选择、后果或简短判断等不同叙述路径；仅替换同义词但保留原句架构不算变化。无新作用的旧细节可以省略。该要求只通过 Prompt 引导，不得以本地文风检测作废完整回复。

低端上下文包含：

- 最近若干轮原始对话。
- 当前场景、位置、在场人物及其公开表演档案。
- 玩家房间、各 NPC 的独立 `mapId + roomId`，以及房间之间的可视与可听关系。
- 中端 AI 当天生成的人物目标、知识边界和对白指导。
- 本地规则层预先确定的回合时长与对应文本预算。
- 最多六条与当前行动相关、且允许在本场景暴露的本地档案记录。

规则层先调用本地 Ollama 语义侧车，以结构化 `playerTurnSequence`、当前时钟、地图路程和事件边界判定经过时间，再要求低端生成足以覆盖该时长的完整现场。侧车必须区分实际动作与对白中的未来提及、假设和回忆；普通 15 分钟回合不能只包含数句对白，但生成篇幅不得反向扩大经过时间。低端不能自行决定经过时间，也无权直接修改权威状态；它只能提交叙事核心与稀疏 proposal，最终变更由结算图验证和应用。

时间校验不得把所有量化相对短语一概视为篡改。`five minutes later`、`ten minutes ago` 等回合内相对叙事，在数值不超过本回合 `elapsedMinutes` 时合法；超出回合跨度的相对时间，以及外部倒计时、绝对钟点、日期、营业时间或交通班次，只有在玩家输入或权威状态明确提供时才合法。Prompt、修复 Prompt 与确定性校验器必须保持同一契约。

### 11.3 中端 AI：每日角色导演

中端 AI 每个游戏日只调用一次，负责：

- 维护 NPC 当前动机、情绪和短期意图。
- 根据事件提出关系变化。
- 形成和更新人物记忆摘要。
- 结算前一日满足的预写线索条件。
- 生成当天人物对白指导、知识焦点和线索机会。
- 生成当天的时间策略；规则层按策略在每回合本地推进时间。

同一游戏日内的普通回合默认不调用中端 AI。每轮先由本地节奏探针检查场景回合数、最近场景核心人物重合度、距上次评估的回合数和待执行转机；未命中时只调用低端现场表演模型。低档前由 1.7B 本地语义侧车以 JSON Schema 结算时长与骰子类别，低档后由同一模型基于英文权威正文稀疏观察物质事件、事件结束和人物活动变化。只有出现明确取得或携带耐久重要物品的候选证据时，才额外调用 4B 的极小背包 Schema。普通回合至少 15 分钟，明确的瞬时动作可以例外。跨入新日期后触发下一次中端日结。用户主动发起的“结束场景”是独立的场景切换事务，不计入普通回合调用频率。

本地语义侧车默认通过 Ollama 运行小型中文指令模型。结构化输出只能保证 JSON 形状，不能直接获得状态写权限；所有字段仍须经过稳定 ID、逐字证据、房间可达性、Schema 和 Reducer 校验。人物活跃在场状态还必须通过地图视线与听觉投影，防止跨地图陈旧人物继续留在当前互动名单。重要物品只接受明确持有证据，普通食物和背景道具不进入背包。前置调用使用 60 秒 `keep_alive`，后置观察完成后立即卸载。Ollama 不可用时遵循 narrative-first：正文照常提交，规则层采用普通短回合时长，跳过不确定骰子、物质事件和事件结束信号，不得吞掉已经生成的叙事。

中端上下文包含前一日对话、相关事件记忆、受保护细节锚点、人物隐藏状态、关系向量和当前可知事实。不同人物的秘密必须保持逻辑隔离，输出结构化日计划。

### 11.3.1 本地世界档案与跨场景 RAG

- 每条时间线在用户数据目录建立独立知识库，按 `actors`、`scenes`、`events`、`clues` 分类保存 JSON。
- 本地 JSON 是权威档案；向量索引只是可重建的检索加速层。
- 人物、场景和线索使用稳定 ID 更新；事件以回合 ID 追加，避免跨场景重新生成已知事实。
- 每次跨场景或生成现场表演前，先按人物、地点、物品和线索 ID 做精确命中，再使用本地 embedding 做语义召回。
- 精确结果优先，语义结果去重补充；单次最多注入六条相关记录。
- 默认使用 SillyTavern 本地 `transformers` embedding，不要求额外 API Key。
- 本地 feature-extraction 单条输入必须限制为 512 tokens；长场景全文保留在权威 JSON 中，向量层不得因 tokenizer 的异常超大 `model_max_length` 将数千 token 一次送入 ONNX WASM。
- 向量检索失败时回退到精确检索，不得阻断存档或丢失本地资料。

### 11.3.2 用户驱动的场景切换

- 场景不会因为低端模型写出移动描写而自动结束；结束主动权属于玩家。
- 中端封存提交的 `transitionMinutes` 仅要求为非负整数，不设本地最大跨度。睡眠、旅行、等待、假期和长期跳时由中端按叙事需要决定；规则层只校验时钟不倒退，不以固定睡眠时长或起床时间作本地拦截。
- 同一小地图内的普通房间移动不等于场景结束。只有玩家输入 `→【地点】`，或在自动候选卡中点击“设为移动”后，规则层才可在低档调用前沿已有出口验证路径并更新 `currentLocalNodeId`；不调用中档，也不写入 `sceneArchive`。
- 自动检测只负责提出候选，不具有状态写入权限。候选卡必须显示“默认不会移动”，并要求玩家在“设为移动 / 仅提及”之间二选一；未确认时阻止提交，避免歧义。
- 输入框必须实时回显已确认移动的起点、终点和明确同行者；地点未知、无可通行路线或跨地图时必须说明原因，避免玩家提交后才发现移动没有成立。
- “跟随某人”“由某人带路”“前往下一个购物点”等明确带路行动允许玩家不知道目标。规则层优先识别点名带路人，再从其公开活动、公开意图和最近两条已提交场景消息中匹配完整房间名、房间 ID 或 alias；目标必须与玩家同图、不是当前房间且路径可达。
- 带路目标解析成功后，玩家、输入中明确同行者和带路人必须在低端调用前作为同一移动事务提交。无法安全确认目标时返回可恢复的 `guide_destination_unknown`，保留所有人物原位置；不得根据地图名猜默认节点。
- 回放或重新生成必须复用已提交移动；但若旧的 `guide_context` 目标与当前严格解析出的公开目标冲突，应从原出发点重放并同步修正同行者，不能让旧错误目的地永久覆盖新权威证据。
- 场所简称默认解析到可继续互动的内部公共节点，例如“古灵阁”进入大厅；只有明确写出入口或台阶时才停在外部入口节点。
- 普通移动只预提交移动语句中明确点名且位于有效路线上的 NPC；带路行动额外提交已解析出的带路人。已经在上下文中明确承诺带队、护送或同行的 NPC 必须由低档在同一回合走完路线，并提交目标 `mapId + roomId` 与新的当前活动。无关角色继续保留原位置。
- 当前房间存在预设固有角色时，规则层在节奏探针之前执行本地准入：缺少人物档案时创建固定档案，缺少运行时实例时写入当前 `mapId + roomId`，重复执行不得生成副本。低端仍只能使用准入后的角色 ID，不能自行创造经营者。
- 普通无聊赶路最多保留一两个简短节拍；只有恋爱、关系张力、冲突、信息推进或即时障碍具有实质价值时才展开途中镜头。无论是否展开，本回合必须抵达目标并继续，不能结束在“仍在路上”“快到了”或“已经看见目的地”。
- 每名 NPC 独立保存 `mapId + roomId`。低档可以处理普通 NPC 走位，但只能在同一张现有地图内沿可通行出口更新位置。
- 不同房间的人物只有在同室、相邻开放出口或地图已声明视线时才能直接看见玩家；可听关系同样由地图路径计算。
- 生成当前场景时，导演必须同时固化玩家可见的默认下一幕意图、目标地图房间和模型档位；生成下一场景时继续预写后续意图，确保场景链不断裂。
- 主输入区提供“封存场景”按钮。弹窗自动填入导演预排意图与档位，玩家可以直接确认，也可以编辑文字、目标地点或切换中端/高端；编辑结果作为显式覆盖记录。
- 玩家意图中命中的现有地点由规则层解析为固定 `mapId + roomId`，中端或高端提案不得改写目标房间。
- 中端切场核心必须包含旧场景收束摘要、公开未决事项、绝对 `nextClock`、经过时间、下一场景 ID、已有地图房间、在场人物公开活动、后续场景意图和 `authorQuillEn`。核心不得包含新场景正文、社交图抽取或 `worldChanges`。
- 中端核心请求使用职责槽位的输出安全余量，并记录 configured/effective max tokens、供应商 `finishReason`、`usage`、输出字符数和结构错误。该余量只防止完整 JSON 被截断，实际消耗仍由真实输出决定。
- 核心校验通过后，低端根据已锁定的时间、房间、人物状态和环境钩子生成 2–6 个下一幕开场分段。低端开场无效时使用规则层短开场，不得回滚封存核心。
- `worldChanges` 在达到 7 天时作为提交后的非阻塞任务生成；存在同房间见证者时创建见证者流言。规则层仍校验新闻时间窗口、人物白名单、知情来源、失真步进和流言衰减。失败保留 pending，可在载入时间线时重试，不得将场景改回失败。
- 旧场景尚未处理的社交声明和 NPC 关系证据在核心提交后交给现有单模型 LangGraph 回填。该任务不得进入场景封存失败边界。
- `authorQuillEn` 是 180–280 词的 OOC 章节后记，产品名为“作者的羽毛笔”。它应以具体玩家行为为素材，包含回收梗、善意吐槽、冷面旁白、虚构奖项或编辑部批注，不得只是复述剧情。
- “作者的羽毛笔”不得侮辱真实玩家，也不得泄露隐藏真相、锁定线索、NPC 私密动机、未来事件、隐藏 DC 或暗骰。它不是世界内事实，不进入任何角色知识或共同记忆。
- 英文后记经现有翻译链生成中文展示，随切场事务原子保存；界面在下一场景开头显示独立 OOC 批注卡，并在旧场景只读档案底部永久保留。
- 规则层校验场景 ID、地图、房间和人物核心后，再校验低端开场或启用确定性短开场，原子提交 `sceneArchive`、当前场景、权威房间和在场人物。提交完成后的 RAG 同步、日计划、室内地图补齐、社交图和世界变化失败不得把 UI 标记为封存失败。
- 下一场景落在 `requires_interior_map` 容器时，提交后调用中端场景制图器；已有 `interiorMapBindings` 时直接进入持久图，不产生模型调用。进入生成式内部图后必须清除父地图遗留的 scope/level 筛选，避免权威位置正确但 UI 仍显示父地图楼层。
- 每个正式新场景必须提交 6–60 词的 `explorationHookEn`，并在开场前两个叙事分段中建立。钩子必须是可观察、可忽略、可检查/跟随/询问/触碰的环境邀请，不得泄露隐藏真相、锁定线索或强制玩家偏离当前目标。同场景进入新房间时，由规则层要求低端补一个同等级的低风险环境钩子。
- 已封存场景在左侧档案栏可重新打开并阅读完整转录，但不显示输入框，也不能继续对话。
- 场景档案按稳定 ID 同步到本地 `scenes/*.json`，跨场景 RAG 检索时保留各自时间、地点和收束摘要。

### 11.3.3 中端节奏监督器

- 普通回合先运行本地节奏探针，不直接消耗模型调用。
- 同一核心阵容连续至少两个场景且当前场景已有互动、单一场景持续至少三个回合，或达到定期复查阈值时，才额外调用一次中端节奏监督器。
- 节奏监督器只返回 `hold` 或一个立即可执行的公开介入，不写现场正文和 NPC 对白。
- 介入类型包括：`environmental_hook` 环境异变、`minor_mishap` 小意外、`urgent_disruption` 可逆紧急情况、`revelation` 已有公开事实或线索显影、`existing_actor_action` 既有人物主动行动，以及普通 `complication / main_arc`。这些类型不得夹带新入场角色。
- `new_actor` 只表示未见人物或新公共 guest；已入库且见过的离场人物重新入场使用 `existing_actor_action`，两类同时参与才使用 `mixed`。探索期优先使用场景合理的未见人物，圈层形成期平衡新人和熟人，社交稳定期才默认熟人优先。熟人重新入场不计入新人份额。
- 选角优先级固定为：玩家本轮明确点名 > 玩家长期追求目标 > 当前因果或未结事件人物 > 当前社交阶段的新人/熟人配额 > 普通熟人召回。本轮明确点名必须复用玩家消息已落盘的结构化受话对象。跨至少两个场景重复定向、且最近三个玩家回合内仍有主动关注，或明确的 friend / mentor / rival / romantic_interest 等关系标签，可以形成长期追求目标；同一场景中的连续对话不会独自产生长期追求。
- 有效认识人数只统计明确关系，或至少跨两个场景且已有关系证据/共同记忆的人；家人和教授不参与阶段人数，一次性店员和同场路人也不计。少于 12 人为探索期（新人/熟人 70/30，普通熟人召回预算 1）；12–15 人为圈层形成期（50/50）；16 人起为社交稳定期（20/80）。家人、教授及其他长期目标仍可通过前三项优先级越过配额；配额只指导中端，不做本地硬拦截。
- 新 Canon 人物必须符合当前年代、地点、公开身份或玩家明确兴趣，并使用目录稳定 ID；社交探索配额可以提高合理候选的优先级，但不能让人物无理由出现。
- 原创公共过场人物是最后手段，并受故事人物总上限与原创人物子上限共同约束。玩家未明确寻找陌生人时不得连续两次新增原创过场人物。
- 地点固有角色由规则层直接准入，不属于节奏监督器的新增人物介入。
- 中端不得借节奏介入发明隐藏真相、锁定线索、特殊能力、永久后果、新地图或原著改写；这些仍属于高端世界导演。
- 中端介入先写入权威 `pacingDirector.pendingBeat`，并由规则层提交获准入场的人物。低端下一次表演必须执行该转机，但不得扩写指令外的新设定。
- 表演成功后将转机标记为已消费；普通自动评估至少等待 6 个已提交回合。全局近龄人物目标只用于发现早期关系缺口，当前场景已有至少 3 名近龄人物时不得继续以该缺口触发中端。玩家明确提出认识新人、寻找其他学生等社交请求仍可绕过自动冷却立即评估。已有待执行转机时禁止重复调用中端。
- 节奏评估失败只记录错误并进入短冷却，不阻断玩家普通回合。

### 11.3.4 中端共同记忆整理器

- 普通回合由低端产生即时人物印象和共同记忆候选，不因此额外调用中端。
- 每个低端提交的 `eventEnded` 小事件边界都会调用一次中端事件边界导演，刷新已经完成或陈旧的 `nextSceneIntent` 文案；这不是场景封存，规则层保留原 `mapId / roomId / tier`，不会自动切场。
- 本地记忆探针不按累计条数轮询；只有事件结束回合距离上次整理至少 10 回合且存在未整理记忆时，才在同一次中端调用中追加共同记忆和社交图整理。冷却期内只刷新下一事件方向，不在第 10 回合补做旧边界。
- 中端只接收当前印象和三层玩家可见共同记忆，不接收秘密来推导公开记忆。
- 整理输出只能引用已有 actor ID 和 memory ID，以 `core / recent / forget` 操作合并、晋升或丢弃记录。
- 整理结果写回 `actorLibrary`，同步进入本地人物 RAG 档案；`actors` 只镜像当前印象和现场状态。
- 整理失败不回滚低端已提交回合；失败边界不会逐回合重试，系统等待下一个事件结束信号。
- 玩家封存章节时，转场导演继续直接执行章节级关系与记忆结算，并清除尚未消费的小事件边界，避免跨章节重复整理。

### 11.3.5 场景级社交导演图

- 社交导演采用单个中端模型与一个 LangGraph 状态图，不拆成多 Agent。客户端负责唯一一次模型抽取，服务端图负责证据收集、来源校验和确定性关系派生。
- 社交抽取与共同记忆整理共用一次中端调用和相同事件边界；章节封存直接复用转场导演已有输出，不增加第二次模型调用。
- NPC 自述家庭、出身、教育、财富、职业、身份、经历或偏好时，系统保存 `claimed` 声明及 `speakerId / witnessedBy / sourceMessageIds / sceneId`，不得直接改写为全知事实。
- 关系证据是有向的，允许 NPC→玩家、玩家→NPC 与 NPC→NPC。端点只能是 `player` 或已入库人物，见证者只能是玩家或引用消息发生时的在场人物。
- 每条声明和关系证据必须引用当前允许的消息 ID。非法人物、伪造消息或越权见证者只导致该条证据被拒绝，不得阻断同批合法记忆、声明或关系。
- 规则层依据新证据幂等派生熟悉度、信任、亲近、张力和保护倾向；稳定证据 ID 防止重放重复计分。模型不得直接提交最终关系分数。
- `socialGraph` 是中央权威记录，人物档案只保存与自身相关的声明和关系投影。低端上下文将声明解释为“某人说过什么”，不能将其当成客观真相。
- 原著人物身份不得被视为 NPC 的世界内知识。低档收到的 `knownRelationshipActorIds` 是已建立人物熟悉度的穷尽列表；列表外人物只能依据本场即时见闻被评价，不得套用未来 Canon 友谊、稳定偏好、昵称、学院关系或内部笑话。
- 人物卡向玩家展示亲自见证的“家庭与背景声明”和“已知人物关系”；摘要优先保留家庭、出身、职业和身份，不得被低价值杂项挤出。
- 旧档当前场景执行一次版本化回填。回填模式必须返回 `scanComplete: true`，规则层强制 `reviews=[]`；截断、修复残片或空壳输出不能清除待回填状态，也不能修改已有共同记忆。

### 11.3.6 世界事实坍塌

- 世界事实坍塌采用延迟物化：玩家首次观测未定的人物、地点、物品或关系时，中端才决定一条在世界时间上早于当前观测、但此前从未被玩家确认的兼容事实。观测触发计算，不是世界内事件的起因。
- 该能力并入现有中端节奏监督器，不建立新模型角色，不并行增加请求。首次深入定向对话、关系阶段升级、显式移动后的地点首次观测、关键/重要物品首次检查可以建立一个坍塌槽位；只有自动节奏冷却已满足时才调用中端。
- 调用频率使用三重限制：自动检查至少间隔 6 个已提交回合；同一槽位只检查一次；每幕最多成功绑定 1 条事实。本轮同时命中普通节奏信号和坍塌槽位时必须合并为同一次 Pacing Director 调用。
- 中端可绑定 `social_edge / offscreen_event / institutional_fact / material_history / obligation / rumor_route`。找不到自然且兼容的事实时，同一次输出降级为 `environmental_hook / minor_mishap / complication / existing_actor_action`，不得返回 hold，也不得追加第二次模型调用。
- 中端只能物化可逆的普通社会关系、场外事件、制度事实、物品历史、旧账和流言渠道。血缘、重大身份、死亡、永久伤害、重大成功犯罪、Canon 改写和其他不可逆事实必须交给高档导演。
- 坍塌必须记录 `effectiveSinceClock / observedAtClock / materializedAtTurn / slotKey / sourceEventIds`。`effectiveSinceClock` 必须早于观测，且不得覆盖玩家已亲历的时间空间、既有对白、人物知识、地图状态或已有坍塌记录。
- 地点类坍塌默认只向低档提供 `visibleResiduesEn / aftermathEn`。旁白不得直接解释隐藏原因；只有被玩家直接询问且拥有对应 witness account 或私有 `causalFacts` 的 NPC 才能说明其所知部分。
- 所有成功坍塌永久写入 `causalCollapse.records` 和本地事件知识库。社会关系同步投影到 `socialGraph`，地点余波进入 `map.roomStates`，物品历史进入对应物品 `causalFactIds`；转机完成后只改变 `pending_surface → surfaced`，不得删除或重新抽取事实。

### 11.4 高端 AI：世界导演

仅在事件触发时调用：

- 创建和维护出场角色库。
- 预写隐藏探索故事线、实际真相、涉事人物和线索图。
- 复杂或有争议的规则裁定。
- 重大选择和永久后果。
- 原著事件条件改变。
- 多条事件线冲突。
- 具有永久后果或复杂因果的场景、章节收束；普通场景切换由中端处理。
- 定期重点角色和世界演算。
- 玩家要求重新裁定创意行动。

高端 AI 不参与每轮普通对话。默认在每日结束时批量演算重点角色和活动事件；重大因果变化发生时额外同步调用。每日演算输入以“上次结算后的状态差异 + 活动事件图 + 重点角色计划”为主，不重复发送无关聊天记录。

### 11.5 上下文策略

每档模型均采用“最近对话 + 权限检索 + 分层摘要”：

1. 最近若干轮原始对话保持当前场景连续性。
2. 根据人物、地点、物品、事件和时间检索相关结构化事实。
3. 检索相关事件记忆、受保护细节锚点和阶段摘要。
4. 根据调用职责过滤秘密和不可见信息。
5. 优先发送状态差异，避免重复发送完整世界。

最近对话轮数和检索预算允许按模型上下文长度配置。完整聊天记录保留在存档中，但不默认全部发送给模型。

默认 Context Size 为 120K，三档职责允许独立覆盖。系统提供三个快捷档位：

- 精简 32K：核心/近期/日常记忆 3/2/1，RAG 3 条，普通近期历史 4 条，章节结算 8 条。
- 标准 64K：记忆 3/4/3，RAG 6 条，普通近期历史 8 条，章节结算 16 条。
- 丰裕 120K：记忆 3/6/8，RAG 10 条，普通近期历史 16 条，章节结算 32 条。

规则层必须至少为 mandatory Prompt 保留 6000 tokens，输出安全余量不得侵占该空间。全局 mandatory Prompt 只保存当前场景、玩家位置、在场人物状态和必要表演边界；完整共同记忆、离场角色、详细地图、隐藏故事线和锁定线索只能进入获得相应权限的职责请求。

所有职责请求依据 `Context Size - 输出安全余量 - mandatory reserve` 计算可用角色预算。Context 较小时优先保留当前行动、规则结果、在场人物核心/近期记忆和精确实体命中，再逐级减少日常记忆、语义 RAG 与旧历史；不得直接截断为无效 JSON。

场景封存采用职责拆分预算：中端核心目标 1200–1800 token，低端开场目标 800–1500 token，跨周世界变化独立按需生成。禁止在一次响应中同时要求核心状态、完整开场、社交图和世界新闻；禁止用同一巨型 Schema 重写作为默认修复策略。

游戏进行中必须提供显式“AI 配置”入口。玩家可以修改低/中/高三档的 Connection Profile、Chat Completion Preset、Regex Preset、Context Size 和输出安全余量，并保存到当前时间线。配置更新后下一次职责调用立即使用新值，不要求退回档案大厅或重开游戏。

### 11.5.1 流式生成反馈

- 低端现场表演使用职责 Profile 的原生流式响应做截断检测与结构恢复，但前端不得把生成中的半截英文、半截句子或 JSON 放入正文阅读区。
- 生成期间只显示固定阶段卡。完整原稿完成翻译并原子提交后，正文自动定位到新回复顶部，再按叙事与对白分段从上到下平滑显影；内容节点不得在动画结束时替换、消失或把读者强制拉到底部。
- 半截 JSON、人物更新和规则字段不得写入聊天或世界状态。只有完整输出通过规则校验后才能原子提交。
- 中端与高端输出继续保持不可见的结构化事务，前端仅显示职责、当前阶段和动态进度反馈。
- 不支持流式的连接自动回退到非流式请求，完成后仍使用相同的顶部定位和分段显影，不得跳过加载反馈或规则校验。
- 所有动效必须支持 `prefers-reduced-motion`，不以动画作为唯一状态提示。

### 11.6 调用失败

- 中端或高端模型失败时暂停相关推进并允许重试。
- 不允许低一级模型越权提交重大或永久状态。
- 玩家可以在暂停界面临时更换模型配置。
- 已产生但未提交的输出不得污染存档。

### 11.7 可见性

默认保持沉浸，只展示叙事和半公开判定。玩家可以展开查看：

- 本轮调用了哪个职责槽位。
- 输入上下文摘要。
- AI 提出的状态变化。
- 规则层接受或拒绝了哪些提案。
- Token、耗时和失败重试。

不得展示 NPC 尚未泄露的秘密或其他越权上下文。

## 12. 状态写入流程

所有会改变世界的行动采用事务流程：

1. 解析玩家意图。
2. 建立当前场景的权限化上下文。
3. 规则层计算确定性约束。
4. 必要时请求中端或高端 AI 提案。
5. 本地规则识别是否需要判定；主动判定按钮可以强制进入该流程。
6. 必要时立即生成并保存权威骰值，计算最终结果。
7. 将既定结果发送给低端模型生成一次完整现场表演。
8. 校验模型是否正确执行判定结果和状态差异。
9. 原子写入人物、关系、物品、时间、事件、判定和事实变化。
10. 自动保存提交点。

AI 文本不是存档。结构化状态和事件日志才是权威来源。

## 13. 界面

### 13.1 主界面

采用“对话流 + 侧栏”：

- 中央：叙事、NPC 对话、判定卡和自由输入框。
- 顶部：日期、时刻、当前地点和场景状态。
- 侧栏：地图、玩家与 NPC 房间标记、在场角色、只读场景档案、人物状态、物品、线索与目标、课程日程。
- 临时覆层：3D 骰盘、重大裁定确认和高端 AI 世界演算。

### 13.2 输入

自由输入是主操作。移动采用 `→【地点】` 作为显式控制语法；普通地点提及只触发可取消的候选确认，不得自动改变权威位置。系统可提供不限制行动的辅助入口：

- 说话。
- 行动。
- 施法。
- 使用物品。
- 移动。
- 观察。
- 申请判定。
- 封存当前场景并前往下一幕。

入口用于帮助解析意图，不应变成固定选项菜单。

### 13.3 判定卡

判定结束后在对话流中保留：

- 行动目标。
- 使用属性和技能。
- 可见修正。
- 骰面与总值。
- 结果等级。
- 直接后果。
- 可展开的规则摘要。

## 14. 存档与时间线

- 每次判定提交或场景结算后自动保存。
- 存档记录状态快照和不可变事件日志。
- 玩家可以复制任一保留的提交点创建分支世界。
- 当前世界不支持无痕覆盖已提交后果。
- 角色退场不等于删除世界。
- 已结束场景保留消息范围、时间、地点、人物和收束摘要，只读可访问。
- 导出内容应包含世界包版本、人物卡、事实库、事件日志和模型配置引用；密钥不得写入导出文件。

## 15. MVP 验收标准

### 15.1 角色与开局

- 玩家能通过结构化问答完成原创角色。
- 玩家能保存故事口味、关系重点、社交密度、相对年龄偏好和 Canon 浓度。
- 玩家能分别填写族裔/可见文化线索与外貌呈现；新 NPC 的第一眼会反映这些信息和自身视角，并与后续行为印象分开保存。
- AI 润色后仍可修改全部已生成内容。
- 人物卡包含六项属性、技能、总体魔法潜质、领域倾向和罕见天赋。
- 分院帽能给出有依据的建议，并接受玩家坚持选择。

### 15.2 游玩

- 玩家可以完成从入学通知到开学首月结束的连续游玩。
- 地图、时间、课程、物品和状态在对话外有结构化记录。
- 自由输入可以触发对话、移动、施法、调查和规则判定。
- 至少三条事件线能在玩家不参与时继续演化。
- 原著人物不会被强制安排为玩家同伴。

### 15.3 判定

- 重要 D20 判定具有可跳过的 3D 动画。
- 规则结果先于叙事生成并成为权威状态。
- 玩家可以在提交前重新裁定。
- 提交后永久后果能在后续场景稳定生效。

### 15.4 关系与秘密

- 12 名重点 NPC 中包含 10 名原著角色和 2 名每局生成的原创角色，并能根据玩家学院替换部分关系位。
- 所有重点 NPC 具有独立动机、事件记忆、秘密和多维关系。
- NPC 能依据底线主动拒绝、欺骗、疏远或敌对玩家。
- 关系变化具有事件来源。
- NPC 出生信息保持固定，年龄层随世界时间和玩家年龄动态计算。
- 固定设定标签与出场后形成的关系标签严格分离。
- NPC 公开介绍的家庭与背景能以带来源声明写回人物卡，不会直接变成全知事实。
- 玩家↔NPC 和 NPC↔NPC 的有向关系都能从可追溯证据派生，并在重复回放时保持幂等。
- 当前场景旧档回填只有在完整扫描标记存在时才完成，失败或截断不会破坏共同记忆。
- 记忆压缩不会丢失被标记的具体礼物、原话、地点和第一次经历。
- 传闻按见证者、传播动机和可信度流动，不会直接变成全局事实。
- 未授权的秘密不会进入低端叙事模型上下文。

### 15.5 AI 调度

- 低、中、高三个槽位可配置，也可复用同一模型。
- 普通日常不会调用高端模型。
- 重大因果变化会触发高端模型。
- 中端或高端模型失败时不会产生半提交状态。
- 玩家可展开查看不泄密的调用和状态变更摘要。

### 15.6 持续世界

- 玩家角色退场后可以在同一世界创建新角色。
- 已改变的原著事实、NPC 关系和事件结果保持不变。
- 复制存档可以建立独立时间线分支。

## 16. 风险与约束

### 16.1 AI 一致性

风险：模型忘记事实、泄露秘密或擅自新增能力。
约束：权限化上下文、结构化输出、规则校验、事件日志和状态写入事务。

### 16.2 成本与延迟

风险：多模型调用导致等待时间和费用过高。
约束：高端仅由重大事件触发；中端按游戏日调用并仅在用户主动普通切场时额外调用；低端承担高频现场表达。

### 16.3 内容规模

风险：完整霍格沃茨蓝本和人物状态迅速膨胀。
约束：只持续模拟重点角色和活动事件，普通人物按需补全，MVP 限制在首月。

### 16.4 规则与 AI 冲突

风险：AI 给出有趣但破坏规则的结果。
约束：AI 只提出创意解释和状态差异，规则层拥有拒绝、降级和要求代价的权力。

### 16.5 IP

当前范围是个人使用和小范围模组分享。不得默认打包来源不明的影视截图、音乐、演员肖像或官方游戏资产。若未来公开发布或商业化，应将引擎与世界包彻底分离，并重新评估名称、文本和素材授权。

## 17. 后续产品决策

以下内容不阻塞 0.1 产品方向，但进入交互原型前需要继续确定：

1. 初始属性的总点数预算、单项上下限和极端属性的背景要求。
2. 固定难度档位、对抗平局和结果等级的精确阈值。
3. 总体魔法潜质与领域倾向的成长倍率和上限公式。
4. 体力轨、压力轨的格数，以及伤势升级和治疗规则。
5. 首月三条以上事件线的具体情节、事件时钟和共享资源。
6. 10 名原著重点 NPC 的具体名单和学院替换规则。
7. 低、中、高模型的默认上下文组成和触发阈值。
8. 记忆细节锚点的自动识别、冲突合并和容量上限。
9. 地图层级、移动耗时和宵禁处理。
10. 角色退场后新角色的接入方式。

## 18. 建议实施阶段

### 阶段一：无 AI 规则原型

- 人物卡。
- 场景、时间和地图。
- D20 判定与 3D 骰盘。
- 状态事务和自动存档。

### 阶段二：单场景 AI 闭环

- 自由输入解析。
- 低端叙事。
- 中端角色导演。
- 权限化秘密。
- 关系变化和人物记忆。

### 阶段三：持续世界

- 日程。
- 事件线。
- 离屏重点角色演算。
- 高端世界导演。
- 原著因果变化。

### 阶段四：首月内容

- 入学全流程。
- 课程和校园日常。
- 重点 NPC。
- 多条可错过事件线。
- 从开局到首月结束的完整验收。
